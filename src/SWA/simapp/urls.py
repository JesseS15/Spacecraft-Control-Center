# STaTE
# File: simapp/urls.py
# Purpose: This file defines url patterns to route user http requests in the simapp Django app

from django.urls import path

from . import views

app_name = 'simapp'
urlpatterns = [
]
