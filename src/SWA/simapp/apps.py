# STaTE
# File: simapp/apps.py
# Purpose: This file defines the app configuration for the simapp Django app

from django.apps import AppConfig

class SimappConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'simapp'
