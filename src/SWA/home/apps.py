# STaTE
# File: home/apps.py
# Purpose: This file defines the app configuration for the home Django app

from django.apps import AppConfig

class HomeConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'home'
